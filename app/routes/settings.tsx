import type { RouteHandle } from "~/types/route";

export const handle: RouteHandle = {
  title: "Configurações",
};

export default function Page() {
  return (
    <div>
      <h1>Settings</h1>
    </div>
  )
}